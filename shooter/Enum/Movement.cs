using Microsoft.Xna.Framework.Input;

namespace shooter.Enum
{
    public enum Movement
    {
        UP = Keys.Up,
        DOWN = Keys.Down,
        LEFT = Keys.Left,
        RIGHT = Keys.Right
    }
}